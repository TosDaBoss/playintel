# Core dependencies
pyyaml>=6.0
pytest>=7.0
pytest-asyncio>=0.21

# Optional: for better output
rich>=13.0
tabulate>=0.9

# Optional: for LLM-as-judge (stubs included, real clients need these)
# openai>=1.0
# anthropic>=0.18
